# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:monospace 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
# font pango:DejaVu Sans Mono 8 
# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
# bindsym $mod+d exec dmenu_run
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop



# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
# bindsym $mod+Shift+j move left
# bindsym $mod+Shift+k move down
# bindsym $mod+Shift+l move up
# bindsym $mod+Shift+semicolon move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# workspace names
# for example workspace numbered 1 = personal chrome windows
set $w1 : <span foreground="#4285F4">media</span>
set $w2 : <span foreground="#EA4335">Firefox</span>
set $w3 : <span foreground="#fbbc05">subl/Atom</span>
set $w4 : <span foreground="#4285f4">video</span>
set $w5 : <span foreground="#34a853">Qgis</span>
set $w6 : <span foreground="#EA4335">Gimp</span>
set $w7 : <span foreground="#EA4335">Nautilus</span>
set $w8 : <span foreground="#EA4330">Others</span>
set $w9 : <span foreground="#831d5f">Spotify</span>
set $w9 : <span foreground="#831d5f">Spotify</span>
set $w10 : <span foreground="#00bcd4">Temp</span>

# switch to workspace
bindsym $mod+1 workspace number 1$w1
bindsym $mod+2 workspace number 2$w2
bindsym $mod+3 workspace number 3$w3
bindsym $mod+4 workspace number 4$w4
bindsym $mod+5 workspace number 5$w5
bindsym $mod+6 workspace number 6$w6
bindsym $mod+7 workspace number 7$w7
bindsym $mod+8 workspace number 8$w8
bindsym $mod+9 workspace number 9$w9
bindsym $mod+0 workspace number 10$w10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number 1$w1
bindsym $mod+Shift+2 move container to workspace number 2$w2
bindsym $mod+Shift+3 move container to workspace number 3$w3
bindsym $mod+Shift+4 move container to workspace number 4$w4
bindsym $mod+Shift+5 move container to workspace number 5$w5
bindsym $mod+Shift+6 move container to workspace number 6$w6
bindsym $mod+Shift+7 move container to workspace number 7$w7
bindsym $mod+Shift+8 move container to workspace number 8$w8
bindsym $mod+Shift+9 move container to workspace number 9$w9
bindsym $mod+Shift+0 move container to workspace number 10$w10

# switch between windows
bindsym $mod+Tab workspace next
bindsym $mod+Shift+Tab workspace prev


# reload the configuration file
bindsym $mod+Shift+c reload

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
	tray_output LVDS-1 # change this between LVDS1/LVDS-1 for nmapple
	font pango: FontAwesome 8
    status_command i3status
    position	top
    separator_symbol "|"
	colors{
            background #10252b
            statusline #a8c9e2
            separator #f4f142
            focused_workspace  #30328f #18191a #a8c9e2
            active_workspace   #63cbc2 #18191a #63cbc2
            inactive_workspace #232323 #18191a #a8c9e2
            urgent_workspace   #FF3B77 #18191a #ff3b77
        }
}

# status_command i3status -c ~/.config/i3/i3status.conf

# SAVIOURS ADDTIONS

# Startup programs
exec --no-startup-id i3-msg exec /opt/firefox_dev/firefox
exec --no-startup-id i3-sensible-terminal
exec --no-startup-id nm-applet
exec --no-startup-id export PATH=/home/sav/anaconda3/bin:$PATH
exec --no-startup-id thunderbird
exec --no-startup-id feh --bg-fill /home/sav/Pictures/bg.png
exec --no-startup-id slack
# exec --no-startup-id sudo mount -ro ntfs /dev/sda2 /mnt/winOS && sudo mount -ro ntfs /dev/sda3 /mnt/datacenter

# Custom KeyBinds
bindsym $mod+t exec nautilus --no-desktop
# bindsym $mod+Shift+l exec i3lock -i ~/Pictures/mybg1.png -o '#191d0f' -w '#572020' -l '#e03e64' -e
bindsym $mod+Shift+l exec i3lock -i ~/Pictures/mybg1.png -e

#screenshot
bindsym Print exec scrot '%Y-%m-%d-%T_$wx$h.png' -e 'mv $f ~/Pictures/screenshots/'

# lauch programs in unique workspaces
# social media
assign [class="Slack"] 1$w1
assign [class="Thunderbird"] 1$w1
assign [class="firefox"] 2$w2
assign [class="Firefox"] 2$w2
# assign [class="Xchat"] 1$w1
# assign [class="Pidgin"] 1$w1

#-----------------------------------------
#text editors
#-----------------------------------------
assign [class="Sublime_text"] 3$w3
assign [class="Atom"] 3$w3

# music players
assign [class="vlc"] 4$w4
assign [class="MPlayer"] 4$w4

assign [class="qgis"] 5$w5
assign [class="Gimp"] 6$w6
assign [class="Thunar"] 7$w7
assign [class="Nautilus"] 7$w7

# spotify
assign [class="spotify"] 9$w9
assign [class="Spotify"] 9$w9

# rofi bindings
bindsym F9  exec rofi -show run    -fg "#f1f1f1" -bg "#333333" -hlfg "#ffffff" -hlbg "#336699" -o 85
bindsym F10 exec rofi -show drun   -fg "#f1f1f1" -bg "#333333" -hlfg "#ffffff" -hlbg "#336699" -o 85
bindsym F12 exec rofi -show window

#--------Rofi Replaces dmenu--------#
bindsym $mod+d exec rofi -show run

